import {demoWorkspace} from './demo'; import type {Workspace} from './types'; const KEY='planora-workspace-v1'; export function loadWorkspace():Workspace{try{const raw=localStorage.getItem(KEY);return raw?JSON.parse(raw):structuredClone(demoWorkspace)}catch{return structuredClone(demoWorkspace)}} export function saveWorkspace(v:Workspace){localStorage.setItem(KEY,JSON.stringify(v))} export function resetWorkspace(){localStorage.removeItem(KEY);return structuredClone(demoWorkspace)}
